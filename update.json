{
  "version": "4.1.7",
  "versionCode": 28,
  "zipUrl": "",
  "changelog": "fix(audio): start pulseaudio with /system/lib64 prepended to LD_LIBRARY_PATH so its system audio chain (libOpenSLES->libgui->libEGL) links against system libEGL instead of Mesa's; resolves the eglDestroySyncKHR link crash that kept the pulse TCP port from opening"
}
